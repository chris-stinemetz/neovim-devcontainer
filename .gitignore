# Environment variables (contains user-specific UID/GID)
.env

# Docker volumes and data
workspace/

# Certificates (optional, user-specific)
config/certs/*.crt
config/certs/*.pem
config/certs/*.cer
config/certs/*.key

# OS files
.DS_Store
*.swp
*.swo
*~